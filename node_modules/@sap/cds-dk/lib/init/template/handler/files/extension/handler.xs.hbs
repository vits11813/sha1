{{#hasTyper}}
const { {{entityNames}} } = require('#cds-models/{{{servicePath}}}')
{{/hasTyper}}
{{^hasTyper}}
const { {{entityNames}} } = this.entities
{{/hasTyper}}

module.exports = async function(req) {
  {{#entityName}}console.log('{{when}} {{event}} {{entityName}}'){{/entityName}}{{^entityName}}console.log('{{when}} {{event}}'){{/entityName}}
}