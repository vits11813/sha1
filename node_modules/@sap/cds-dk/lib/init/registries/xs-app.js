

const odataApi = {
  in: 'routes',
  where: { target: '/odata/$1' }
}

const html5RepoApi = {
  in: 'routes',
  where: { service: 'html5-apps-repo-rt' }
}

module.exports = {
  odataApi,
  html5RepoApi
}
