const cds = require('../cds')
const { compile } = cds._local('@cap-js/openapi') // load a local cap-js/openapi (fallback to our own)

function cds_compile_to_openapi (csn, options) {
  return compile(csn, options)
}

module.exports = cds_compile_to_openapi
